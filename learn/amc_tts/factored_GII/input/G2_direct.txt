# OPTIONAL: AMC DIRECT with P kept (expand check). Prefer strip/restore:
#   G2_Wdirect_noperm.txt + G2_Vdirect_noperm.txt  (NOTES.md).
#
# Unfactored Γ^II = expand of Path B:
#   G2 = Σ_a [P(i/j) χ_ja Ω_iakl − P(k/l) χ_ak Ω_ijal]
#   χ_ij = 1/2 Σ_abc w Γ_aibc Ω_bcaj
# Packaging: unreduced Γ and G2. Occ w same as χ^ζ.

declare Omega {
    mode=4,
    latex="\\Omega",
    scalar=false
}

declare Gamma {
    mode=4,
    latex="\\Gamma",
    scalar=true
}

declare n {
    mode=2,
    diagonal=true,
    latex="n"
}

declare nbar {
    mode=2,
    diagonal=true,
    latex="\\bar{n}"
}

declare G2 {
    mode=4,
    latex="\\Gamma^{(II)}",
    scalar=true
}

G2_ijkl = 1/2 * sum_abcd((n_a*n_b*nbar_c + nbar_a*nbar_b*n_c)
    * (P(i/j) * Gamma_ajbc * Omega_bcad * Omega_idkl
     - P(k/l) * Gamma_adbc * Omega_bcak * Omega_ijdl));
