# Path B: Pandya(Ω) → product in Pandya → inv Pandya → χ^γ
# Packaging locked: Ω tensor reduced; barChi/chi scalar reduce=true
#
# AMC default sample uses chi = - barChi. For this TT→0 topology that overall
# minus makes Path B = −(m / AMC direct). Correct equation uses chi = + barChi
# (drop one overall minus). Locked by m-scheme: see OMEGA_TT_TO_SCALAR.md
declare Omega {
    mode=4,
    latex="\\Omega",
    scalar=false
}
declare barO {
    mode=4,
    latex="\\bar{\\Omega}",
    scalar=false,
    scheme=((1,-4),(3,-2))
}
barO_ijkl = - Omega_ijkl;

declare n { mode=2, diagonal=true, latex="n" }
declare nbar { mode=2, diagonal=true, latex="\\bar{n}" }

declare barChi {
    mode=4,
    latex="\\bar{\\chi}^{\\gamma}",
    scalar=true,
    reduce=true,
    scheme=((1,-4),(3,-2))
}
barChi_ijkl = sum_ab((n_a*nbar_b*n_j*nbar_k - nbar_a*n_b*nbar_j*n_k) * (barO_ajkb*barO_ibal));

declare chi {
    mode=4,
    latex="\\chi^{\\gamma}",
    scalar=true,
    reduce=true
}
# Corrected: drop AMC-sample overall minus (that minus → Path B = −direct/m)
chi_ijkl = barChi_ijkl;
