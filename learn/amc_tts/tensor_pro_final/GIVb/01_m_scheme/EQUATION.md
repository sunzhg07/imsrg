# $\Gamma^{\mathrm{IV}_b}$ / $\chi^\iota$ — m-scheme

Status: bare χ **DONE** (m ≡ AMC); fold / Path B open

Trusted m (η-analog / Factorized `bar_CHI_V`) — **not** arxiv:

$$
\chi^\iota_{ijkl}
=\sum_{ab}
\bigl(\bar n_a n_b\bar n_k+n_a\bar n_b n_k\bigr)
\,\Omega_{bjka}\,\Gamma_{iabl}
$$

Arxiv $\Omega_{bika}\Gamma_{iabl}$ leaves free $j$ off the RHS. See `factored_GIV/NOTES.md`.
