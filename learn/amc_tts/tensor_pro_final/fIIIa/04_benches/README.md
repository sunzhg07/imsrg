# $f^{\mathrm{III}_a}$ / $\chi^\gamma$ — three-way benches (m ≡ AMC direct ≡ Path B)

Status: **DONE**

Gold: m ≡ AMC direct ≡ Path B for χ^γ; then ladder.

## Scripts (from repo `run/`)

```bash
cd /Users/wolf/work/imsrg
export PYTHONPATH=build
python3 -B run/test_chi_gamma_pathB_amc.py 1 2   # emax λ — see script docstring
python3 -B run/test_chi_gamma_mscheme.py 1 2   # emax λ — see script docstring
python3 -B run/test_tts_fIIIa_mscheme.py 1 2   # emax λ — see script docstring
python3 -B run/test_tts_fIIIa.py 1 2   # emax λ — see script docstring
```

Linked copies under `scripts/` point at the live `run/` files.

