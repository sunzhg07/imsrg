# $\Gamma^{\mathrm{III}_c}$ / $\chi^\theta$ — three-way benches (m ≡ AMC direct ≡ Path B)

Status: **DONE**

Gold: m ≡ AMC DIRECT ≡ Path B.

## Scripts (from repo `run/`)

```bash
cd /Users/wolf/work/imsrg
export PYTHONPATH=build
python3 -B run/test_chi_theta_mscheme.py 1 2   # emax λ — see script docstring
python3 -B run/test_tts_GIIIc_mscheme.py 1 2   # emax λ — see script docstring
python3 -B run/test_tts_GIIIc_new_direct.py 1 2   # emax λ — see script docstring
```

Linked copies under `scripts/` point at the live `run/` files.

