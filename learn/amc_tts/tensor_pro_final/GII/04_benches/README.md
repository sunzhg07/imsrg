# $\Gamma^{\mathrm{II}}$ / $\chi^\zeta$ — three-way benches (m ≡ AMC direct ≡ Path B)

Status: **DONE** — m ≡ DIRECT ≡ Path B ≡ ethS (incl. λ≠0)

m ≡ DIRECT ≡ Path B @λ=0 DONE.

## Scripts (from repo `run/`)

```bash
cd /Users/wolf/work/imsrg
export PYTHONPATH=build
python3 -B run/test_chi_zeta_mscheme.py 1 2   # emax λ — see script docstring
python3 -B run/test_tts_GII_pathB_mscheme.py 1 2   # emax λ — see script docstring
python3 -B run/test_tts_GII_direct_mscheme.py 1 2   # emax λ — see script docstring
python3 -B run/test_tts_GII.py 1 2   # emax λ — see script docstring
python3 -B run/test_tts_GII_eths_pathB.py 1 2   # emax λ — see script docstring
```

Linked copies under `scripts/` point at the live `run/` files.

