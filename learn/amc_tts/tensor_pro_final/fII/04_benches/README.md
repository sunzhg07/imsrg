# $f^{\mathrm{II}}$ / $\chi^\beta$ — three-way benches (m ≡ AMC direct ≡ Path B)

Status: **DONE**

Gold: m ≡ DIRECT ≡ Path B.

## Scripts (from repo `run/`)

```bash
cd /Users/wolf/work/imsrg
export PYTHONPATH=build
python3 -B run/test_tts_f_mscheme.py 1 2   # emax λ — see script docstring
python3 -B run/test_tts_fII.py 1 2   # emax λ — see script docstring
python3 -B run/test_chi_beta_mscheme.py 1 2   # emax λ — see script docstring
```

Linked copies under `scripts/` point at the live `run/` files.

