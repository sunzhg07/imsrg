# $\Gamma^{\mathrm{IV}_c}$ / $\chi^\lambda$ — three-way benches (m ≡ AMC direct ≡ Path B)

Status: **DONE**

Gold: m ≡ ring ≡ Path B. ethS Path B only.

## Scripts (from repo `run/`)

```bash
cd /Users/wolf/work/imsrg
export PYTHONPATH=build
python3 -B run/test_chi_lambda_mscheme.py 1 2   # emax λ — see script docstring
python3 -B run/test_tts_GIVc_mscheme.py 1 2   # emax λ — see script docstring
python3 -B run/test_tts_GIVc_pathB.py 1 2   # emax λ — see script docstring
python3 -B run/test_tts_GIVc.py 1 2   # emax λ — see script docstring
```

Linked copies under `scripts/` point at the live `run/` files.

