# OBSOLETE arxiv print — do NOT use as gold for χ^κ.
# Trusted equation: factorized_code_analyze.tex eq:chikappa
#   → input/chi_kappa_analyze.txt  (Ω_aicd Γ_jcba, occ on (a,c,d))
#
# Old form kept only for historical AMC compare:
#   χ_ijkl = Σ_ab (n̄_a n_b n_k + n_a n̄_b n̄_k) Ω_ajbl Γ_ibka

declare Omega {
    mode=4,
    latex="\\Omega",
    scalar=false
}

declare Gamma {
    mode=4,
    latex="\\Gamma",
    scalar=true
}

declare n {
    mode=2,
    diagonal=true,
    latex="n"
}

declare nbar {
    mode=2,
    diagonal=true,
    latex="\\bar{n}"
}

declare Chi {
    mode=4,
    latex="\\chi^{\\kappa}",
    scalar=false
}

Chi_ijkl = sum_ab(
    (nbar_a*n_b*n_k + n_a*nbar_b*nbar_k) * Omega_ajbl * Gamma_ibka);
