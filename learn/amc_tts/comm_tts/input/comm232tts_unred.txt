# comm232tts: tensor 2b × tensor 3b → scalar 2b  [Z UNREDUCED]
# Packaging:
#   X, Y: scalar=false (WE-reduced tensors)
#   Z: scalar=true, reduce=false
# Occ: N_abc = n_a n_b nbar_c + nbar_a nbar_b n_c (comm232st pattern).
# Post-AMC (by hand): ×(1/2), (1-P_ij^{J0}) on terms 1&3, (1-P_kl^{J1}) on terms 2&4,
#   δ_{J0,J1}, (-1)^{j_i/j_k}/Ĵ_0 factors per learn/threebody_commutator equations.tex.
# Ref: index map J0↔J1_code, J1↔J2_code, J2↔J3, j0↔j1, j1↔j2 in that file.

declare X {
    mode=4,
    latex="X",
    scalar=false
}

declare Y {
    mode=6,
    latex="Y",
    scalar=false
}

declare Z {
    mode=4,
    latex="Z",
    scalar=true,
    reduce=false
}

declare n {
    mode=2,
    diagonal=true,
    latex="n"
}

declare nbar {
    mode=2,
    diagonal=true,
    latex="\\bar{n}"
}

# Terms [1] and [3] pattern (before X/Y swap)
Z_ijkl = sum_abc((n_a*n_b*nbar_c+nbar_a*nbar_b*n_c)*X_cjab*Y_abiklc);
Z_ijkl = sum_abc((n_a*n_b*nbar_c+nbar_a*nbar_b*n_c)*Y_ijcabk*X_abcl);

declare X {
    mode=6,
    latex="X",
    scalar=false
}

declare Y {
    mode=4,
    latex="Y",
    scalar=false
}

declare Z {
    mode=4,
    latex="Z",
    scalar=true,
    reduce=false
}

# Terms [2] and [4] pattern (X 3b × Y 2b)
Z_ijkl = sum_abc((n_a*n_b*nbar_c+nbar_a*nbar_b*n_c)*Y_cjab*X_abiklc);
Z_ijkl = sum_abc((n_a*n_b*nbar_c+nbar_a*nbar_b*n_c)*X_ijcabk*Y_abcl);
